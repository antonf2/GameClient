<Window x:Class="UserManagement.AddEditUser"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UserManagement"
        Background="GhostWhite"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="User Info" Height="450" Width="400">
    <Viewbox>
        <Grid>
            <StackPanel>
                <TextBlock Text="Username" Margin="10"/>
                <TextBox x:Name="UsernameText" Margin="10"/>
                <TextBlock Text="Description" Margin="10"/>
                <TextBox x:Name="DescriptionText" Margin="10"/>
                <TextBlock Text="Image URL" Margin="10"/>
                <TextBox x:Name="URLText" Text="/Assets/profile.png" Margin="10"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                    <Button Content="OK" Width="75" Margin="5" Click="OkButton_Click" Background="#FF0ABB0A"/>
                    <Button Content="Cancel" Width="75" Margin="5" Click="CancelButton_Click" Background="#FFF5A7A7"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Viewbox>
</Window>
